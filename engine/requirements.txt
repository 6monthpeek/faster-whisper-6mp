# Core transcription engine
faster-whisper==1.2.1

# Web server
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9
sse-starlette>=1.6.0

# Model downloading & progress tracking
huggingface-hub>=0.24.0
tqdm>=4.66.0

# CUDA / hardware detection
ctranslate2>=4.0.0
torch>=2.0.0

# Speaker diarization (no HF token required — uses SpeechBrain directly)
speechbrain>=1.0.0

# Noise suppression
noisereduce>=3.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# System info
psutil>=5.9.0

# Build tool
pyinstaller>=6.15.0
